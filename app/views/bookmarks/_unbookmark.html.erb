<% if params[:controller] == Settings.blogs_s || params[:type] == Settings.blog_s %>
<%= link_to '<i class="fa fa-bookmark" aria-hidden="true"></i>'.html_safe, bookmark_path(id: bookmark.ids), method: :delete, remote: true %>
<% else %>
<%= link_to '<i class="fa fa-bookmark fa-2x" aria-hidden="true"></i>'.html_safe, bookmark_path(id: bookmark.ids), method: :delete, remote: true %>
<% end %>
